from setuptools import setup, find_packages

setup(
    name="hyper-lattice-protocol",
    version="9.0.1",
    packages=find_packages(),
    author="Omni-Manifold Committee",
    description="AGI Alignment Kernels",
)
